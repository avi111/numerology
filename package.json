{
  "name": "maya259",
  "version": "1.0.0",
  "author": "Avi Levkovich <avi111@gmail.com>",
  "repository": "https://github.com/avi111/numerology.git",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "bootstrap": "lerna run rimraf && lerna run tsc --stream && lerna bootstrap --hoist",
    "start": "lerna run start --stream",
    "tsc": "lerna run tsc --stream",
    "watch": "lerna run watch --stream",
    "dev": "lerna run dev",
    "dev-logs": "lerna run dev --parallel",
    "dev-module": "lerna run dev --scope",
    "test": "lerna run test",
    "storybook": "lerna run storybook",
    "production": "lerna run production",
    "build": "lerna run build",
    "deploy": "lerna run deploy",
    "rimraf": "rm -rf node_modules",
    "installApp": "yarn rimraf && yarn install && yarn bootstrap"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
